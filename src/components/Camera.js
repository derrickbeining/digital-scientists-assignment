import Component from '../Component';
import {hasMediaDeviceAPI, startingCamera} from '../mediaDevice';

const Camera = new Component('Camera', {
  show: false
})

Camera.render((state) => {

})

export default Camera;
